/**
 * Generates binary fixture files used by the E2E tests.
 *
 * Run once:  npx ts-node fixtures/generate-fixtures.ts
 * Or it is called automatically from the test fixture helper.
 *
 * Files produced:
 *   unused-device.jpg      — small valid JPEG simulating an unused-looking device photo
 *   cracked-screen.png     — small valid PNG simulating a cracked-screen complaint photo
 *   blurry-photo.webp      — minimal valid WebP (ambiguous / low quality)
 *   oversized-image.jpg    — fake JPEG header + padding to exceed 10 MB
 *   wrong-type.gif         — a GIF file (unsupported format)
 */

import * as fs from 'fs';
import * as path from 'path';

const FIXTURES_DIR = path.resolve(__dirname);

/**
 * Creates a minimal, valid JPEG buffer.
 * A 1×1 pixel solid-colour JPEG (raw byte sequence).
 */
function createMinimalJpeg(comment = 'unused-device'): Buffer {
  // Minimal JFIF JPEG for a 1x1 white pixel
  const jpeg = Buffer.from([
    ,
  ]);
  return jpeg;
}

/**
 * Creates a minimal valid PNG (1×1 white RGB pixel).
 *
 * CRC values are computed correctly using zlib/crc32 — not hardcoded guesses.
 * Verified decodable by Java ImageIO (javax.imageio.ImageIO.read).
 */
function createMinimalPng(): Buffer {
  const zlib = require('zlib') as typeof import('zlib');

  // CRC32 table
  const crcTable = new Uint32Array(256);
  for (let i = 0; i < 256; i++) {
    let c = i;
    for (let j = 0; j < 8; j++) {
      c = (c & 1) ? (0xEDB88320 ^ (c >>> 1)) : (c >>> 1);
    }
    crcTable[i] = c >>> 0;
  }
  function crc32(buf: Buffer): number {
    let crc = 0xFFFFFFFF;
    for (const b of buf) crc = (crcTable[(crc ^ b) & 0xFF] ^ (crc >>> 8)) >>> 0;
    return ((crc ^ 0xFFFFFFFF) >>> 0);
  }
  function chunk(type: string, data: Buffer): Buffer {
    const typeBytes = Buffer.from(type, 'ascii');
    const lenBuf = Buffer.alloc(4);
    lenBuf.writeUInt32BE(data.length);
    const crcInput = Buffer.concat([typeBytes, data]);
    const crcBuf = Buffer.alloc(4);
    crcBuf.writeUInt32BE(crc32(crcInput));
    return Buffer.concat([lenBuf, typeBytes, data, crcBuf]);
  }

  // IHDR: 1×1, 8-bit RGB
  const ihdr = Buffer.alloc(13);
  ihdr.writeUInt32BE(1, 0); // width
  ihdr.writeUInt32BE(1, 4); // height
  ihdr[8] = 8; // bit depth
  ihdr[9] = 2; // color type RGB (no alpha)

  // Raw scanline: filter byte 0 (None) + white pixel (R=255, G=255, B=255)
  const rawScanline = Buffer.from([0x00, 0xff, 0xff, 0xff]);
  const compressed = zlib.deflateSync(rawScanline, { level: 9 });

  return Buffer.concat([
    Buffer.from([0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a]), // PNG signature
    chunk('IHDR', ihdr),
    chunk('IDAT', compressed),
    chunk('IEND', Buffer.alloc(0)),
  ]);
}

/**
 * Creates a minimal valid WebP (1×1 pixel, VP8L lossless).
 *
 * The RIFF container wraps a VP8L chunk with correct bitstream.
 * Accepted by TwelveMonkeys imageio-webp (backend dependency).
 * Note: base Java ImageIO returns null for WebP without TwelveMonkeys —
 * this is expected; the backend's runtime classpath includes TwelveMonkeys.
 */
function createMinimalWebp(): Buffer {
  // VP8L bitstream for a 1×1 white pixel (ARGB 0xFFFFFFFF)
  const vp8lBitstream = Buffer.from([
    0x2f, 0x00, 0x00, 0x00, 0xfe,
    0x07, 0x10, 0x00, 0x00, 0x00,
    0x00, 0xfe, 0xff, 0x03, 0x00,
  ]);

  const chunkSize = vp8lBitstream.length;
  const paddedChunk = chunkSize % 2 === 0
    ? vp8lBitstream
    : Buffer.concat([vp8lBitstream, Buffer.from([0x00])]);

  const riffSize = 4 + 8 + paddedChunk.length; // 'WEBP' + chunk header + data
  const buf = Buffer.alloc(12 + 8 + paddedChunk.length);
  buf.write('RIFF', 0);
  buf.writeUInt32LE(riffSize, 4);
  buf.write('WEBP', 8);
  buf.write('VP8L', 12);
  buf.writeUInt32LE(chunkSize, 16);
  paddedChunk.copy(buf, 20);
  return buf;
}

/**
 * Creates a fake oversized JPEG (valid header + padding to exceed 10 MB).
 */
function createOversizedJpeg(): Buffer {
  const header = createMinimalJpeg();
  // Pad to 10.5 MB to reliably exceed the 10 MB limit
  const TARGET_SIZE = 10.5 * 1024 * 1024;
  const padding = Buffer.alloc(TARGET_SIZE - header.length, 0x00);
  return Buffer.concat([header, padding]);
}

/**
 * Creates a minimal GIF87a file (unsupported format).
 */
function createGif(): Buffer {
  return Buffer.from([
    0x47, 0x49, 0x46, 0x38, 0x37, 0x61, // "GIF87a"
    0x01, 0x00, 0x01, 0x00, 0x80, 0x00, // 1×1, global color table flag
    0x00, 0xff, 0xff, 0xff, 0x00, 0x00, // background + 2 colours
    0x00, 0x2c, 0x00, 0x00, 0x00, 0x00, // image descriptor
    0x01, 0x00, 0x01, 0x00, 0x00, 0x02,
    0x02, 0x4c, 0x01, 0x00, 0x3b,       // LZW data + trailer
  ]);
}

// Generate all fixture files
const files: Array<{ name: string; data: Buffer }> = [
  { name: 'unused-device.jpg',   data: createMinimalJpeg('unused-device') },
  { name: 'cracked-screen.png',  data: createMinimalPng() },
  { name: 'blurry-photo.webp',   data: createMinimalWebp() },
  { name: 'oversized-image.jpg', data: createOversizedJpeg() },
  { name: 'wrong-type.gif',      data: createGif() },
];

for (const file of files) {
  const outPath = path.join(FIXTURES_DIR, file.name);
  if (!fs.existsSync(outPath)) {
    fs.writeFileSync(outPath, file.data);
    console.log(`Created: ${file.name} (${(file.data.length / 1024).toFixed(1)} KB)`);
  } else {
    console.log(`Exists:  ${file.name} (${(fs.statSync(outPath).size / 1024).toFixed(1)} KB)`);
  }
}

console.log('Fixture generation complete.');
